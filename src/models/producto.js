class Producto {
    constructor(nombre, precio, stock, proveedorId) {
        this.nombre = nombre;
        this.precio = precio; // En pesos colombianos (COP)
        this.stock = stock;
        this.proveedorId = proveedorId;
    }
}